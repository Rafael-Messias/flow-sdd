interface:
  display_name: "Validate Tasks"
  short_description: "Validate task metadata before execution."
  default_prompt: "Validate the task files for this workflow and fix metadata issues until validation passes."
